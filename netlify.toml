# 모든 혼란을 끝낼 최종 netlify.toml (v3.0)

[build]
  # 웹사이트의 기본 파일들이 있는 곳 (index.html 등)
  publish = "."
  # 서버리스 함수들이 있는 곳
  functions = "netlify/functions"

# '/api/*' 주소를 실제 함수 주소로 연결하는 리디렉션 규칙
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200